# 这里存放后续文章的计划和想法

## AI相关

### 【AI】大模型XXX和lora微调实验：以Qwen3:0.6B为例（未开始）

首先介绍下特殊标记token

优化总体步骤
我们以Qwenx:xxB为例，它支持非常多的语言，但是我们实际用不到，且白白浪费空间和性能。 因此尝试去除不需要的语言的词汇，引入一些需要领域的词汇作为词表，然后微调模型让它更好的发挥性能。

去除不需要的词汇
加入新增词汇(使用相近含义的词汇加权得出)
使用lora训练词汇参数和层参数
使用数据集验证训练结果

总结
刚接触还不熟悉

```
## 训练数据准备
## 训练代码
## 实验和结果
```

### 【AI】大模型的蒸馏与后训练：以Qwen3:0.6B为例（未开始）

普通后训练
普通蒸馏
自己的模型使用百分比蒸馏


### 【AI】使用强化学习进行后训练

### 学习和用代码实现一个最简单的神经网络（不用库纯自己写代码）

### （后期）了解大模型中常用的网络结构

### （大后期）自己预训练后训练一个mini模型
大幅简化词表和层数，但原理和基础结构还是按照大模型的形式。让基本所有电脑都能跑的起来。
可以不是来理解和生成自然语言，而是一种简化的任务，但原理和使用形态上和自然语言的大模型一致。
如果做的好有教育意义可以作为项目推广开源。

有一个不太好的，但可以作为参考的想法：
以数字和运算符作为词表。使用大模型训练来解数学题。但是这样体现不出多轮对话上下文的性质。所以再想想。


